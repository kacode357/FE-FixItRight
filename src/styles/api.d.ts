// src/types/api.d.ts (hoặc bất kỳ nơi nào bạn tổ chức các kiểu dữ liệu)
export interface LoginResponse {
    accessToken: string;
    // Bạn có thể thêm các trường khác nếu cần thiết
    // ví dụ: userId, username, etc.
  }
  